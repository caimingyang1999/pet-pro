# 宠迹 - 宠物档案系统项目参考文档

## 一、项目概述

### 1.1 项目名称
**宠迹** - 宠物档案管理系统

### 1.2 项目描述
宠迹是一个集宠物档案管理、宠物资讯社区、积分商城于一体的综合服务平台。用户可以在平台上记录和管理自己的宠物信息，浏览和发布宠物相关动态，通过积分商城兑换宠物用品。

### 1.3 系统架构
系统分为三个部分：
- **小程序端**：面向普通用户
- **后台管理端**：面向系统管理员
- **后台服务端**：提供API接口服务

---

## 二、技术栈

### 2.1 小程序端
| 技术 | 版本 | 说明 |
|------|------|------|
| UniApp | 最新版 | 跨平台开发框架 |
| Vue 3 | 3.x | 前端框架 |
| Vite | 最新版 | 构建工具 |
| uView Plus | 3.x | UI组件库（适配Vue3） |
| Pinia | 2.x | 状态管理 |

### 2.2 后台管理端 & 服务端
| 技术 | 说明 |
|------|------|
| RuoYi-Vue | 基础框架 |
| Spring Boot | 后端框架 |
| Vue 2 + Element UI | 前端框架 |
| MySQL | 数据库 |
| MyBatis-Plus | ORM框架 |
| Spring Security + JWT | 权限认证 |

---

## 三、小程序端功能设计

### 3.1 导航结构（Tab页）
```
├── 首页（动态资讯）
├── 商城（积分商城）
├── 爱宠（宠物管理）
└── 我的（个人中心）
```

---

### 3.2 首页 - 动态资讯

#### 3.2.1 功能描述
展示宠物相关的资讯动态，用户可以浏览、发布、点赞、评论动态。

#### 3.2.2 页面结构
```
首页
├── 顶部搜索栏
├── 轮播图/Banner（热门话题、活动）
├── 动态列表（瀑布流/信息流）
│   ├── 动态卡片
│   │   ├── 发布者头像、昵称
│   │   ├── 发布时间
│   │   ├── 动态文字内容
│   │   ├── 图片/视频展示（九宫格）
│   │   ├── 关联宠物标签
│   │   ├── 点赞按钮
│   │   ├── 评论按钮
│   │   └── 分享按钮
│   └── 加载更多
├── 发布动态悬浮按钮
└── 下拉刷新
```

#### 3.2.3 发布动态页面
```
发布动态页
├── 文字内容输入区
├── 图片/视频上传区（最多9张）
├── 关联宠物选择（可选）
├── 位置信息（可选）
└── 发布按钮
```

---

### 3.3 商城 - 积分商城

#### 3.3.1 功能描述
用户通过积分兑换宠物用品，包含商品浏览、详情查看、兑换功能。

#### 3.3.2 页面结构
```
商城首页
├── 积分余额展示
├── 商品分类导航
│   ├── 食品
│   ├── 玩具
│   ├── 护理用品
│   ├── 服饰
│   └── 其他
├── 商品列表
│   ├── 商品卡片
│   │   ├── 商品图片
│   │   ├── 商品名称
│   │   ├── 所需积分
│   │   └── 库存状态
│   └── 加载更多
└── 商品详情页
    ├── 商品轮播图
    ├── 商品名称、所需积分
    ├── 商品详情描述
    ├── 库存数量
    ├── 兑换记录
    └── 立即兑换按钮
```

---

### 3.4 爱宠 - 宠物管理

#### 3.4.1 功能描述
管理用户的宠物信息，支持添加、编辑、查看详情、删除宠物档案。

#### 3.4.2 页面结构
```
爱宠列表页
├── 宠物卡片列表
│   ├── 宠物头像
│   ├── 宠物名称
│   ├── 品种信息
│   ├── 年龄
│   └── 点击查看详情/编辑
├── 添加宠物按钮
└── 空状态提示

宠物详情/编辑页
├── 宠物头像上传
├── 宠物名称
├── 宠物品种（选择器）
├── 出生日期（日期选择器）
├── 性别（单选）
├── 体重
├── 毛色
├── 绝育状态
├── 疫苗记录（子表单）
│   ├── 疫苗名称
│   ├── 接种日期
│   └── 下次接种日期
├── 备注信息
└── 保存/编辑/删除按钮
```

#### 3.4.3 宠物信息字段
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 宠物头像 | 图片 | 否 | 宠物照片 |
| 宠物名称 | 文本 | 是 | 宠物名字 |
| 品种 | 选择 | 是 | 猫/狗等品种 |
| 出生日期 | 日期 | 否 | 宠物生日 |
| 性别 | 单选 | 否 | 公/母 |
| 体重 | 数字 | 否 | 单位kg |
| 毛色 | 文本 | 否 | 毛发颜色 |
| 绝育状态 | 单选 | 否 | 已绝育/未绝育 |
| 疫苗记录 | 子表 | 否 | 疫苗接种记录 |

---

### 3.5 我的 - 个人中心

#### 3.5.1 页面结构
```
我的页面
├── 用户信息卡片
│   ├── 头像
│   ├── 昵称
│   └── 积分余额
├── 功能入口
│   ├── 我的动态
│   ├── 我的兑换订单
│   ├── 积分明细
│   ├── 收货地址管理
│   └── 设置
├── 我的动态列表
│   ├── 发布的动态列表
│   └── 支持删除操作
└── 兑换订单列表
    ├── 订单状态筛选
    ├── 订单信息展示
    └── 订单详情
```

---

## 四、后台管理端功能设计

### 4.1 功能模块

#### 4.1.1 动态管理
- 动态列表查询（支持按用户、时间、状态筛选）
- 动态详情查看
- 动态审核（通过/拒绝）
- 动态删除
- 评论管理

#### 4.1.2 商城管理
- **商品分类管理**
  - 分类增删改查
  - 分类排序
- **商品管理**
  - 商品列表查询
  - 商品新增/编辑
  - 商品上下架
  - 商品删除
  - 库存管理
- **订单管理**
  - 兑换订单列表
  - 订单状态变更（待发货/已发货/已完成/已取消）
  - 订单详情查看

#### 4.1.3 宠物列表管理
- 宠物信息列表查询
- 宠物详情查看
- 宠物信息编辑（管理员权限）
- 宠物删除（违规处理）

#### 4.1.4 用户管理（基于RuoYi原有功能扩展）
- 用户列表（关联宠物数量、动态数量）
- 用户积分管理
- 积分明细查看
- 积分充值/扣减

---

## 五、API接口设计

### 5.1 通用接口规范

#### 请求格式
- Base URL: `/api/v1`
- Content-Type: `application/json`
- 认证方式: Bearer Token (JWT)

#### 响应格式
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {}
}
```

### 5.2 动态模块接口

#### 5.2.1 动态列表
```
GET /api/v1/posts
```
**请求参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | int | 是 | 页码 |
| pageSize | int | 是 | 每页条数 |
| keyword | string | 否 | 搜索关键词 |
| userId | long | 否 | 用户ID（我的动态） |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "userId": 1,
        "userName": "张三",
        "avatar": "/avatar/xxx.jpg",
        "content": "今天带猫咪去洗澡了~",
        "images": ["/post/img1.jpg", "/post/img2.jpg"],
        "petId": 1,
        "petName": "咪咪",
        "likeCount": 10,
        "commentCount": 3,
        "isLiked": true,
        "createTime": "2026-01-01 12:00:00"
      }
    ]
  }
}
```

#### 5.2.2 发布动态
```
POST /api/v1/posts
```
**请求体：**
```json
{
  "content": "动态内容",
  "images": ["图片URL数组"],
  "petId": 1
}
```

#### 5.2.3 动态详情
```
GET /api/v1/posts/{id}
```

#### 5.2.4 删除动态
```
DELETE /api/v1/posts/{id}
```

#### 5.2.5 点赞/取消点赞
```
POST /api/v1/posts/{id}/like
```

#### 5.2.6 评论列表
```
GET /api/v1/posts/{id}/comments
```

#### 5.2.7 发表评论
```
POST /api/v1/posts/{id}/comments
```
**请求体：**
```json
{
  "content": "评论内容",
  "parentId": 0
}
```

### 5.3 宠物模块接口

#### 5.3.1 我的宠物列表
```
GET /api/v1/pets
```

#### 5.3.2 添加宠物
```
POST /api/v1/pets
```
**请求体：**
```json
{
  "name": "咪咪",
  "avatar": "/pet/xxx.jpg",
  "breed": "英短",
  "birthday": "2025-01-01",
  "gender": "1",
  "weight": 4.5,
  "color": "蓝白",
  "sterilization": "1",
  "vaccines": [
    {
      "vaccineName": "猫三联",
      "inoculationDate": "2025-06-01",
      "nextDate": "2026-06-01"
    }
  ],
  "remark": "很乖的小猫咪"
}
```

#### 5.3.3 宠物详情
```
GET /api/v1/pets/{id}
```

#### 5.3.4 更新宠物信息
```
PUT /api/v1/pets/{id}
```

#### 5.3.5 删除宠物
```
DELETE /api/v1/pets/{id}
```

### 5.4 商城模块接口

#### 5.4.1 商品分类列表
```
GET /api/v1/shop/categories
```

#### 5.4.2 商品列表
```
GET /api/v1/shop/products
```
**请求参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| categoryId | long | 否 | 分类ID |
| keyword | string | 否 | 搜索关键词 |
| pageNum | int | 是 | 页码 |
| pageSize | int | 是 | 每页条数 |

#### 5.4.3 商品详情
```
GET /api/v1/shop/products/{id}
```

#### 5.4.4 兑换商品
```
POST /api/v1/shop/orders
```
**请求体：**
```json
{
  "productId": 1,
  "quantity": 1,
  "addressId": 1
}
```

#### 5.4.5 兑换订单列表
```
GET /api/v1/shop/orders
```

#### 5.4.6 订单详情
```
GET /api/v1/shop/orders/{id}
```

### 5.5 用户模块接口

#### 5.5.1 积分明细
```
GET /api/v1/user/points/log
```

#### 5.5.2 收货地址管理
```
GET /api/v1/user/addresses
POST /api/v1/user/addresses
PUT /api/v1/user/addresses/{id}
DELETE /api/v1/user/addresses/{id}
```

---

## 六、数据库设计

### 6.1 数据库ER图关系
```
sys_user (RuoYi用户表)
    │
    ├──1:N──→ pet_info (宠物信息表)
    │
    ├──1:N──→ pet_post (动态表)
    │
    ├──1:N──→ shop_order (兑换订单表)
    │
    ├──1:N──→ user_address (收货地址表)
    │
    └──1:N──→ user_points_log (积分记录表)

pet_post (动态表)
    │
    └──1:N──→ post_comment (评论表)

shop_product (商品表)
    │
    ├──N:1──→ shop_category (商品分类表)
    │
    └──1:N──→ shop_order (订单表)
```

### 6.2 建表SQL

```sql
-- 1. 宠物信息表
CREATE TABLE pet_info (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID（关联sys_user）',
    name VARCHAR(50) NOT NULL COMMENT '宠物名称',
    avatar VARCHAR(255) COMMENT '宠物头像',
    breed VARCHAR(50) NOT NULL COMMENT '品种',
    birthday DATE COMMENT '出生日期',
    gender CHAR(1) COMMENT '性别（0-母 1-公）',
    weight DECIMAL(5,2) COMMENT '体重(kg)',
    color VARCHAR(30) COMMENT '毛色',
    sterilization CHAR(1) DEFAULT '0' COMMENT '绝育状态（0-未绝育 1-已绝育）',
    remark VARCHAR(500) COMMENT '备注',
    del_flag CHAR(1) DEFAULT '0' COMMENT '删除标志（0-存在 1-删除）',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='宠物信息表';

-- 2. 疫苗记录表
CREATE TABLE pet_vaccine (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    pet_id BIGINT NOT NULL COMMENT '宠物ID',
    vaccine_name VARCHAR(100) NOT NULL COMMENT '疫苗名称',
    inoculation_date DATE COMMENT '接种日期',
    next_date DATE COMMENT '下次接种日期',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    INDEX idx_pet_id (pet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='疫苗记录表';

-- 3. 动态表
CREATE TABLE pet_post (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '发布用户ID',
    pet_id BIGINT COMMENT '关联宠物ID',
    content TEXT NOT NULL COMMENT '动态内容',
    images JSON COMMENT '图片列表（JSON数组）',
    status CHAR(1) DEFAULT '0' COMMENT '审核状态（0-待审核 1-通过 2-拒绝）',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    comment_count INT DEFAULT 0 COMMENT '评论数',
    view_count INT DEFAULT 0 COMMENT '浏览数',
    del_flag CHAR(1) DEFAULT '0' COMMENT '删除标志',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id),
    INDEX idx_pet_id (pet_id),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='动态表';

-- 4. 动态评论表
CREATE TABLE post_comment (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    post_id BIGINT NOT NULL COMMENT '动态ID',
    user_id BIGINT NOT NULL COMMENT '评论用户ID',
    parent_id BIGINT DEFAULT 0 COMMENT '父评论ID（0-一级评论）',
    content VARCHAR(500) NOT NULL COMMENT '评论内容',
    del_flag CHAR(1) DEFAULT '0' COMMENT '删除标志',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    INDEX idx_post_id (post_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='动态评论表';

-- 5. 动态点赞表
CREATE TABLE post_like (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    post_id BIGINT NOT NULL COMMENT '动态ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_post_user (post_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='动态点赞表';

-- 6. 商品分类表
CREATE TABLE shop_category (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    parent_id BIGINT DEFAULT 0 COMMENT '父分类ID',
    category_name VARCHAR(50) NOT NULL COMMENT '分类名称',
    icon VARCHAR(255) COMMENT '分类图标',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status CHAR(1) DEFAULT '1' COMMENT '状态（0-停用 1-正常）',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品分类表';

-- 7. 商品表
CREATE TABLE shop_product (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    category_id BIGINT NOT NULL COMMENT '分类ID',
    product_name VARCHAR(100) NOT NULL COMMENT '商品名称',
    product_images JSON COMMENT '商品图片（JSON数组）',
    description TEXT COMMENT '商品描述',
    points_price INT NOT NULL COMMENT '积分价格',
    stock INT DEFAULT 0 COMMENT '库存数量',
    total_exchange INT DEFAULT 0 COMMENT '总兑换数',
    status CHAR(1) DEFAULT '1' COMMENT '状态（0-下架 1-上架）',
    del_flag CHAR(1) DEFAULT '0' COMMENT '删除标志',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    INDEX idx_category_id (category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';

-- 8. 兑换订单表
CREATE TABLE shop_order (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    order_no VARCHAR(32) NOT NULL COMMENT '订单编号',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    product_id BIGINT NOT NULL COMMENT '商品ID',
    product_name VARCHAR(100) COMMENT '商品名称（快照）',
    product_image VARCHAR(255) COMMENT '商品图片（快照）',
    points_price INT NOT NULL COMMENT '兑换积分',
    quantity INT DEFAULT 1 COMMENT '兑换数量',
    total_points INT NOT NULL COMMENT '总积分',
    address_id BIGINT COMMENT '收货地址ID',
    status CHAR(1) DEFAULT '0' COMMENT '订单状态（0-待发货 1-已发货 2-已完成 3-已取消）',
    express_no VARCHAR(50) COMMENT '快递单号',
    express_company VARCHAR(50) COMMENT '快递公司',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_order_no (order_no),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='兑换订单表';

-- 9. 用户收货地址表
CREATE TABLE user_address (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    receiver_name VARCHAR(50) NOT NULL COMMENT '收件人',
    receiver_phone VARCHAR(20) NOT NULL COMMENT '联系电话',
    province VARCHAR(50) COMMENT '省份',
    city VARCHAR(50) COMMENT '城市',
    district VARCHAR(50) COMMENT '区县',
    detail_address VARCHAR(200) NOT NULL COMMENT '详细地址',
    is_default CHAR(1) DEFAULT '0' COMMENT '是否默认（0-否 1-是）',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户收货地址表';

-- 10. 积分记录表
CREATE TABLE user_points_log (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    points_change INT NOT NULL COMMENT '积分变动（正数为增加，负数为扣减）',
    points_balance INT NOT NULL COMMENT '变动后余额',
    change_type VARCHAR(20) NOT NULL COMMENT '变动类型（sign_in-签到 post-发布动态 exchange-兑换商品 admin-管理员操作）',
    relate_id BIGINT COMMENT '关联业务ID',
    remark VARCHAR(200) COMMENT '备注',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分记录表';
```

---

## 七、项目结构

### 7.1 小程序端结构
```
pet-trace-miniapp/
├── src/
│   ├── api/                    # API接口封装
│   │   ├── post.js            # 动态接口
│   │   ├── pet.js             # 宠物接口
│   │   ├── shop.js            # 商城接口
│   │   └── user.js            # 用户接口
│   ├── components/            # 公共组件
│   │   ├── PostCard.vue       # 动态卡片组件
│   │   ├── PetCard.vue        # 宠物卡片组件
│   │   ├── ProductCard.vue    # 商品卡片组件
│   │   └── UploadImage.vue    # 图片上传组件
│   ├── pages/
│   │   ├── index/             # 首页（动态）
│   │   │   ├── index.vue
│   │   │   └── publish.vue
│   │   ├── shop/              # 商城
│   │   │   ├── index.vue
│   │   │   └── detail.vue
│   │   ├── pet/               # 爱宠
│   │   │   ├── list.vue
│   │   │   └── edit.vue
│   │   └── mine/              # 我的
│   │       ├── index.vue
│   │       ├── posts.vue
│   │       ├── orders.vue
│   │       └── address.vue
│   ├── store/                 # 状态管理
│   │   ├── user.js
│   │   └── pet.js
│   ├── utils/                 # 工具函数
│   │   ├── request.js         # 请求封装
│   │   └── index.js
│   ├── App.vue
│   ├── main.js
│   ├── pages.json
│   ├── manifest.json
│   └── uni.scss
├── package.json
└── vite.config.js
```

### 7.2 服务端结构（RuoYi扩展）
```
ruoyi-pettrace/
├── ruoyi-admin/
│   └── src/main/java/com/ruoyi/web/controller/
│       ├── pet/               # 宠物模块
│       │   └── PetInfoController.java
│       ├── post/              # 动态模块
│       │   ├── PostController.java
│       │   └── CommentController.java
│       └── shop/              # 商城模块
│           ├── ShopCategoryController.java
│           ├── ShopProductController.java
│           └── ShopOrderController.java
├── ruoyi-system/
│   └── src/main/java/com/ruoyi/system/
│       ├── domain/
│       │   ├── PetInfo.java
│       │   ├── PetVaccine.java
│       │   ├── PetPost.java
│       │   ├── PostComment.java
│       │   ├── PostLike.java
│       │   ├── ShopCategory.java
│       │   ├── ShopProduct.java
│       │   ├── ShopOrder.java
│       │   ├── UserAddress.java
│       │   └── UserPointsLog.java
│       ├── mapper/
│       │   ├── PetInfoMapper.java
│       │   ├── PetPostMapper.java
│       │   └── ShopProductMapper.java
│       └── service/
│           ├── IPetInfoService.java
│           ├── IPostService.java
│           └── IShopService.java
└── sql/
    └── pettrace.sql           # 项目数据库脚本
```

---

## 八、积分规则设计

| 行为 | 积分变动 | 说明 |
|------|----------|------|
| 每日签到 | +5 | 每天首次签到 |
| 发布动态 | +10 | 审核通过后发放 |
| 动态被点赞 | +1 | 每天上限20分 |
| 动态被评论 | +2 | 每天上限20分 |
| 完善宠物信息 | +20 | 首次完善 |
| 兑换商品 | -对应积分 | 积分兑换扣除 |

---

## 九、开发排期建议

| 阶段 | 内容 | 预估工期 |
|------|------|----------|
| 第一阶段 | 数据库设计、基础环境搭建 | 3天 |
| 第二阶段 | 用户认证对接、宠物管理模块 | 5天 |
| 第三阶段 | 动态发布与展示模块 | 5天 |
| 第四阶段 | 积分商城模块 | 5天 |
| 第五阶段 | 后台管理端开发 | 5天 |
| 第六阶段 | 联调测试、修复优化 | 3天 |
| **合计** | | **约26天** |

---

## 十、注意事项

1. **小程序端**使用UniApp开发时，注意条件编译，为后续发布其他平台做准备
2. **图片上传**建议对接云存储服务（如阿里云OSS、腾讯云COS）
3. **积分商城**需要做好库存扣减的并发控制
4. **动态内容**建议接入内容审核（微信官方或第三方）
5. **RuoYi框架**二次开发时，注意在原有代码基础上扩展，不要修改核心代码
6. **数据库**使用utf8mb4字符集，支持emoji表情存储

---

> 📌 本文档为项目参考文档，具体实现可根据实际需求进行调整。