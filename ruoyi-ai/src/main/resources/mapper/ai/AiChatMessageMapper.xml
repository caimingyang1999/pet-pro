<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiChatMessageMapper">

    <resultMap type="com.ruoyi.ai.domain.AiChatMessage" id="AiChatMessageResult">
        <id     property="id"           column="id"            />
        <result property="sessionId"    column="session_id"    />
        <result property="userId"       column="user_id"       />
        <result property="role"         column="role"          />
        <result property="content"      column="content"       />
        <result property="contentType"  column="content_type"  />
        <result property="tokenUsed"    column="token_used"    />
        <result property="model"        column="model"          />
        <result property="status"       column="status"        />
        <result property="errorMsg"     column="error_msg"     />
        <result property="delFlag"      column="del_flag"       />
        <result property="createTime"   column="create_time"   />
        <result property="updateTime"   column="update_time"   />
        <result property="sessionTitle" column="session_title" />
    </resultMap>

    <sql id="selectMessageVo">
        select m.id, m.session_id, m.user_id, m.role, m.content, m.content_type, m.token_used,
               m.model, m.status, m.error_msg, m.del_flag, m.create_time, m.update_time,
               s.session_title
        from ai_chat_message m
        left join ai_chat_session s on m.session_id = s.id
    </sql>

    <select id="selectMessageListBySessionId" resultMap="AiChatMessageResult">
        <include refid="selectMessageVo"/>
        <where>
            m.del_flag = '0'
            <if test="sessionId != null">
                and m.session_id = #{sessionId}
            </if>
        </where>
        order by m.create_time asc
    </select>

</mapper>
